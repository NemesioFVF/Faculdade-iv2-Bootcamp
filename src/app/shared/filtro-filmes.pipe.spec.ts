import { FiltroFilmePipe } from './filtro-filmes.pipe';

describe('FiltroFilmePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroFilmePipe();
    expect(pipe).toBeTruthy();
  });
});
